name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: cmake configure
      run: cmake configure
    - name: cmake hedracore
      run: cmake hedracore
    - name: dir
      shell: bash
      run: ls
